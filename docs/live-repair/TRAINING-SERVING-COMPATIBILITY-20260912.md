# Explicit compatibility decision before the main trajectory

Train and select on the unchanged, twice-reviewed v4 24/8 snapshot. The completed four-layer proof used exactly that snapshot, the pinned tokenizer, `enable_thinking=false`, assistant-only JSON targets, citation aliases, loss and full sequence lengths. Do not replace those reviewed inputs or falsely claim byte identity with the current product prompt.

The current product system prompt is newer: SUPPLIED EVIDENCE replaces VERIFIED SOURCES; profile residence no longer proves jurisdiction; record provenance is more explicit; up to six concise sentences and anti-citation-loop instructions replace the older short-answer wording. These are retained product repairs, not reasons to restore the old serving policy. Role markers, non-thinking assistant prefix, JSON answer/citation schema and alias syntax remain the same interface. Exact system bytes differ (reviewed training system 9,317 characters); the claim `serving_prefix_matched` in the runner refers only to tokenizer/template and final-target format, not identical system prose.

Decision: proceed with the authorized, unchanged reviewed-data training experiment and its matching proof. No new target, source mapping or data review is manufactured. Learned behavior under the old prompt may not transfer perfectly to the newer product policy; actual current-serving answer/browser evaluation remains required before promotion. Validation replay under the dataset prompt is selection evidence only, never current-product acceptance. This explicitly records the distribution difference instead of using it as another permission loop or silently modifying reviewed data.
