# Resource, resume and answer-boundary repair

## Latest continuation — V8 data approved, larger-workload proof failed

The owner continued execution. One source-checked v7 target correction was followed by one final v8 correction after a newly identified omitted operative effect. Both failed intermediate reviews are preserved. V8 now has32PASS from each pinned isolated reviewer; only train008/train009 changed from v6, all input messages and the complete validation file are unchanged. Exact serving-wrapper preflight passes: longest3625, maximum completion93, max_seq_length3680, no dropped source or target tokens. Review root: `/Users/hltsang/.codex/private/pension-adaptability/20260912/dataset-v8-review`.

The main entrypoint now accepts `--checkpointed`, using the same complete-state engine as the diagnostic. It requires an exact-workload successful proof before any main spawn. New development tests cover finite main/disposable budgets, immutable resource ceilings, proof receipt mutation/incompatibility, silent stage deadlines and owned-group cleanup without stopping another group. Full safe suite280/280 passed. This is implemented/component-tested main integration, NOT a successfully executed new main trajectory.

One new changed-workload proof ran from preserved lineage19 with fresh Adam, four trainable layers and full3680 envelope at `/Users/hltsang/.codex/private/pension-adaptability/20260912/checkpointed-v8-proof`. At start swap3464.56MiB/free56%; during first compile/update swap reached6646.69MiB (growth3182.13 versus2048 cap), free9% (floor10), process footprint8.9926GiB. All nine required telemetry samples were valid. The last interval recorded348.52MiB/s system-wide swap-outs. The supervisor stopped its child with SIGTERM, elapsed45.182seconds; both child/supervisor are gone. No completed update, checkpoint, validation/reload or new candidate comparison. The actual full-gradient MLX peak is unavailable because the model stopped before emitting its next event; the earlier4.6465GB event is NOT the gradient peak.

This is real resource pressure under the tested larger context/current host, not an authorization deficit, missing metric or the previously repaired baseline-delta bug. It does not prove a memory leak or exclusive model ownership of all system swap. The earlier3292-token proof cannot override this failure. This continuation's proof attempt is consumed; main12 and its24-generation comparison remain NOT_RUN, with no automatic retry or ceiling increase. Historical main work stays30 and latest durable lineage19; original104 is unchanged. A distinct lower-memory configuration needs a measured, bounded proposal/proof, not another identical restart.

Independent lightweight work: all12 previously seeded PDU50 stores passed a read-only comparison of original account facts, user ownership, record versions, document checksums, exact spans/locators and unchanged DB hashes. Receipt: `/Users/hltsang/.codex/private/pension-adaptability/20260912/pdu50-seed-readonly-audit/receipt.json`. All12 use degraded embeddings, so pinned semantic-index readiness is not established. Fixture-label review/auth/browser remain pending; no case was marked ready or passed and no stored DB was changed. No new formal run, unseen access, commit/push or deployment.

The earlier sections below retain the preceding turn's observations; current state is above and in STATUS.json.

Development work only. Checkpoint104 and the historical main/recovery receipts are preserved. The owner explicitly requested this repair and bounded training/comparison; another identical authorization request is not a prerequisite. Sealed unseen remains excluded.

## Resume correctness

The old recovery saved adapter and optimizer arrays but not the active sampling permutation/cursor or RNG state. Its saved lineage19 cannot be described as an exact continuation. The new `ml/adaptability_resume.py` checkpoint stores the full adapter, optimizer tree, MLX/Python/NumPy RNG states and a private shuffle RNG with epoch, permutation, cursor and completed-update count. Saves use a staging directory, fsync and atomic rename. Identity mismatch, altered tensor bytes, invalid sampler exposure and incomplete paths are rejected.

CPU test: uninterrupted12 versus5 + process exit +7 produces bit-identical parameters, optimizer and RNG arrays and identical sample order, across an epoch boundary. This is synthetic CPU equivalence, not a claim of bit-exact 8B GPU equivalence.

New bounded full-model diagnostic: one attempt, two disposable updates, fresh process for the second update and a third process for reload/full8 validation and one generation. Root: `/Users/hltsang/.codex/private/pension-adaptability/20260912/resume-state-proof-v2`. It retains the reviewed full-context smoke examples, baseline104, complete16-layer adapter and four trainable layers. No main-lineage credit or model promotion.

Completed: all3 processes exit0, two finite-gradient updates saved, full8 validation finite (weighted loss1.2334237817), one generation ended normally. Total442.28seconds. First/second update MLX peaks9.0303/7.3489GB; fresh validation/generation peak6.0303GB. Maximum observed swap growth1823.13MiB, below2048MiB but with limited headroom. This is a functioning new-format restart proof, not evidence that this disposable adapter should be served.

The first preparation (`resume-state-proof-v1`) rejected before spawning a model because the new supervisor initially measured swap growth from zero rather than the observed host baseline. This was a code bug, not actual excessive growth. The rejected receipt is preserved; v2 corrects the baseline and a regression test covers it. It did not consume a full-model attempt.

## Resource evidence and remaining limits

Old recovery:192 native samples; baseline4440.06MiB swap, maximum7156.94MiB, growth2716.88MiB exceeded2048MiB; maximum observed process footprint7.770GiB. Missing per-stage swap counters mean these historical observations cannot allocate the system-wide growth between model work and unrelated processes.

New telemetry records native process footprint, timed probe errors, system swap usage, compressor pages, swap-in/out rates, and the last model phase with MLX active/cache/peak readings. MLX values are event-time measurements, not live measurements inside a compiled step. VM counters are system-wide, not per-process swap attribution.

The new proof's observed swap-outs cluster around phase1 compile/update and save:181.44MiB,1264.69MiB and514.25MiB in the respective sampled intervals. This supports transient first-gradient pressure; it does not prove a persistent leak or exclusive model ownership of system swap. Fresh-process reload can preserve completed work while releasing process allocations, but a longer trajectory and the revised3625-token workload remain unproven.

Limits remain: prestart6144MiB, absolute8192MiB, growth2048MiB, free10%, process12GiB, MLX14 decimal GB; this diagnostic has1800seconds total. No unrelated application was stopped and no global OS setting changed. A short successful proof does not guarantee that a longer training segment will fit.

## Answer correction and bounded comparison

Shared policy v15 now requires explanation of supplied scheme terms without contradictory blanket disclaimers; it also requires an explicit cannot-determine conclusion when material facts are missing. It does not demand uncertainty when the evidence resolves the question. These are shared rules, not case-ID response patches.

The new comparison is frozen under `evidence-boundary-comparison-v1`: baseline104 and saved lineage12, same current product system policy and context builder, existing8 validation questions/sources,16 generations maximum,256 output tokens,4 isolated reviewer calls including existing positive/negative controls. No target answers or PDU50 evaluator content are included in prompts. This is a prompt-bound development replay, not browser acceptance or qualification. It does not reinterpret the historical6/8 result as a new PASS.

Completed comparison: baseline3/8 and lineage12 candidate5/8 dual-review PASS. Candidate003,005,006 retain unsupported qualifications or categorical denials; candidate006 explicitly denies a recipient outcome the source merely leaves unresolved. The two requested answer problems are NOT demonstrated fixed. Baseline had two256-token length stops; candidate had eight normal stops, which does not establish correctness. No promotion. This replay uses direct generation at temperature0 and256 tokens; unlike product transport it omits the literal `/no_think` prefix, temperature0.1/320 defaults and retry/recovery. It tests shared policy/context, not byte-identical serving or browser behavior. No controlled old-policy/current-context A/B was performed, so the difference from historical6/8 cannot be attributed solely to policy text.

The new checkpoint helper is exercised by the dedicated proof only. The legacy main continuation route is NOT yet migrated to complete-state checkpoints. A speculative unexecuted generic runner was removed before handoff rather than presented as a tested integration. The next main segment needs an integrated supervisor/driver, a versioned reviewed dataset and matching workload proof; old proof receipts do not authorize a changed workload automatically.

## Versioned target correction and shape gate

The first full-preservation migration (`dataset-v5-format-migration`) retains all source blocks, including secondary distractors, history, questions and original targets. Shared policy and task formatting change, so the old dataset review is not transferable. Fresh reviewers found18PASS/14HOLD and27PASS/5HOLD: sentence-level citation mapping, unnecessary follow-ups, and missing explicit cannot-determine conclusions. These receipts are preserved.

One bounded correction (`dataset-v6-target-correction`) changes14 training targets and3 development-validation references following those findings; all input messages and split membership are byte-preserved. No validation example enters gradients, no formal/PDU50/sealed gold changes, and historical scores retain their original reference version. Its separate two-reviewer result is terminal HOLD: A32PASS, B31PASS/1HOLD. The remaining item train009 omits the source's prerequisite checks and the qualifying-earnings option with its stated contribution minima. This dataset is NOT approved for training. No additional until-green review or main run was launched.

Exact serving-wrapper tokenization finds a longest3625-token row, exceeding the existing3584 ceiling; four training rows fail full-target coverage. No sources or targets were truncated, no new main update was launched. A larger untruncated workload requires its own resource proof before training; a tokenizer-only PASS would not establish that proof. Earlier core-only probes that lost secondary sources/history are explicitly marked NOT_TRAINING_DATA.

The safe development suite passed275/275, including the fresh-process CPU resume test. This is component evidence only.

Final reconciliation at2026-09-12T14:02:14Z: no owned training/model/review worker remains, and ports3001/8080/8090 have no listeners. System swap used3680.56MiB at this snapshot. No model promotion, deployment, commit/push or sealed access. Remaining work is source-faithful target repair/review, exact-workload proof and integration of full-state main continuation, followed by bounded effectiveness assessment and the separate PDU50/browser/qualification gates; none is an authorization deficit.

## PDU50 preparation

Twelve fixture-only cases now have separate seeded SQLite stores and hash-bound preparation receipts under `pdu50-fixture-preparation`. Independent fixture-label binding, authentication/session checks and browser execution remain pending.

Three GOV.UK guidance captures (contributions, changing jobs/leave, joining) were reviewed by both pinned isolated reviewers. The first joining proposal incorrectly included re-enrolment; reviewerA held it. The repaired scope excludes re-enrolment, preserves the same fetched bytes and original HOLD, and both reviewers now pass all3 narrowly scoped sources. This is not approval of case-as-of law, territorial applicability, legal labels or active-corpus admission. No PDU50 legal case has been silently marked ready.

Current receipts and completion status are recorded in `STATUS.json`. Product/browser/formal completion must not be inferred from component tests, source review or training.
