# Local development runbook — 8 September 2026 repair

## Latest execution: resource/resume and evidence-boundary repair, 12 September

Latest17:18Z: `lineage31-comparison-v1` is COMPLETE, not resumable/rerunnable. 24 generations and6 isolated reviews yielded baseline1/8, parent192/8, lineage315/8; no hard-gate-clean candidate. Workers have exited and services remain offline. Preserve checkpoint104 and all results; next work is a fresh bounded shared-behavior repair for003 unsupported No,006 invented missing terms,008 omitted human confirmation, not more automatic gradients or formal qualification. See [result](live-repair/LINEAGE31-COMPARISON-20260912.md).

Latest15:42Z overrides historical stop/approval instructions below: owner-delegated bounded adjustments are authorized without repeat confirmation; verification supplement COMPLETE, bounded main12 COMPLETE, all model processes exited. Main work42 / durable lineage31, full8 validation and normal-stop generation; no promotion. Next work is semantic baseline104/parent19/lineage31 comparison, not another gradient restart or repeat of consumed attempts. Effective main validation420s is bound in its delegated-control.json; RAM/swap guards unchanged. Read [latest result](live-repair/DELEGATED-CONTINUATION-RESULT-20260912.md) and STATUS.json. Safe development285PASS is not browser/qualification acceptance.

Latest15:14Z: `checkpointed-v8-bounded-memory-proof` has TWO completed/saved disposable updates, but is terminal STAGE_TIMEOUT_full_validation after6/8 cases. Memory guards passed; no worker remains. Do not repeat gradients or start main. The exact next proposal is one verification-only continuation with validation180→300s and unchanged memory limits; read [result and amendment proposal](live-repair/MEMORY-REPAIR-RESULT-20260912.md). Original failure receipts stay immutable.

Latest14:58Z: `checkpointed-v8-two-layer-proof` is terminal FAILED_SWAP_GROWTH_LIMIT (2793.81MiB/2048), zero updates, no active owned training process. Its one attempt is consumed; do not rerun it or launch main. The explicit `--two-layer` configuration and full-state route are implemented, but actual feasibility remains unproven. See [result](live-repair/TWO-LAYER-RESULT-20260912.md).

Latest continuation overrides the earlier pending-code/data wording below: V8 dataset is dual32PASS and tokenizer3680 PASS. `continueAdaptabilityStudy.mjs --checkpointed` now supports full-state per-update continuation, but the new `checkpointed-v8-proof` failed on swap growth3182.13MiB/free9% before any update. Its attempt is terminal/consumed. Do not invoke main or retry that root; no new matching proof exists. Main30/durable19 and checkpoint104 remain unchanged. PDU50 read-only seed audit passed12/12 but found degraded embeddings in all12, not semantic-index readiness. Current service is offline and no owned model/review worker remains. Read STATUS.json and actual receipts before further work.

The new two-update disposable resume proof completed successfully across three fresh processes, including full8 validation and generation, under unchanged memory/swap ceilings. See [the current repair record](live-repair/RESUME-AND-ANSWER-REPAIR-20260912.md) and [live status](live-repair/STATUS.json) before acting. The separate shared-policy/context baseline104/lineage12 comparison completed3/8 versus5/8; it is not full product transport, browser acceptance or a successful answer repair. Main-lineage work remains30 confirmed /19 durable; diagnostic updates are separate. No model has been promoted. The new full-state helper is proven in the diagnostic, not yet integrated into the legacy main continuation entrypoint. Revised full-context data exceeds the old sequence cap and needs a matching resource proof. Older status/PID/approval wording below is historical, not a new authorization requirement.

Current state: no running training process. Read `live-repair/STATUS.json` and reconcile receipts before any continuation. The free10 main terminated on unavailable footprint metrics after23 confirmed updates. Its weight-only recovery at `/Users/hltsang/.codex/private/pension-adaptability/20260912/recovery-from12` terminated on SWAP_GROWTH_LIMIT after7 additional, durably saved updates. Total confirmed main work30; latest weight lineage19; no selected adapter. Training permission is already present; do not ask for the same authorization again.

Both attempts are terminal and their runners refuse reuse. Do not invoke the completed24-update recovery selection path against these interrupted artifacts. Preserve original104 and all failed receipts. The latest complete full-adapter/optimizer pair is `durable-updates/007/` in the recovery root; the optimizer was explicitly reset at lineage12. RNG/dataloader state was not saved, so even these optimizer files are not proof of an exact deterministic full-state resume. No further gradient restart was scheduled. First address host resource admissibility and define an honest bounded continuation or partial-candidate evaluation. Never label missing checkpoint24/36/48 or unrun answer review as passed.

The separate zero-gradient partial baseline-versus12 assessment has now completed:16 generated answers,4 isolated reviewer calls, dual-review pass3/8→6/8, exact loss1.25413→1.16184. One candidate source-contradiction hard failure remains; selection decision is NO_FULLY_ASSESSED_HARD_GATE_CLEAN_CANDIDATE. Receipts are under `20260912/partial-baseline-vs12`; do not rerun that consumed assessment or describe it as qualification. No model is active and no new adapter has been promoted.

This candidate is development-only and not qualified. See [current report](LIVE-ASSISTANT-REPAIR-REPORT.md), [status](live-repair/STATUS.json) and [model diagnosis](live-repair/MODEL-DIAGNOSIS.md). The previous runbook is preserved under `live-repair/history/`.

## Current 12 September training authority

The latest user explicitly authorizes amendments and automatic training. See `live-repair/OWNER-TRAINING-AMENDMENT-20260912.json`; older approval-required wording below is historical and must not create another permission loop. The new `20260912/run-v3-final` preparation revalidates the existing two-reviewer dataset receipt, binds a standalone longest row separately from the two-row smoke file, and retains finite resource guards. Execute the completed two-update proof before main training; no PDU50 legal-label prerequisite applies to training. Preparation is not an executed optimizer update.

The original Desktop v3 pack was verified byte-for-byte against all 19 project files, then moved recoverably to `/Users/hltsang/.Trash/Pension_Dashboard_v3_Pack-20260912`. The root pack contract is unchanged; the canonical owner contract has the dated current-user amendment pointer.

## 9 September owner contract and actual training boundary

Read [OWNER-EXECUTION-CONTRACT.md](OWNER-EXECUTION-CONTRACT.md) explicitly before continuing. It supersedes the earlier no-training wording below only for the reviewed, bounded 24/8 and maximum 48-update experiment. The original 104 adapter remains the baseline, not a qualified release. Current Git origin is `Tsanhl/Pensions-dashboard-.git`, different from the owner's historical repository label; the requested local checkout and existing repair artifacts were preserved without changing the remote.

The 8 September smoke ended in a system reset after initial validation; no optimiser update, adapter or normal exit was demonstrated. Both 9 September disposable probes were stopped by their declared host swap-growth guard during gradients, with zero completed updates. Their private receipts are under `/Users/hltsang/.codex/private/pension-adaptability/20260909/diagnostic-eager/` and `diagnostic-target-loss/`. The two-full-model diagnostic budget is exhausted. This is a bounded resource-proof failure, not evidence that this hardware can never train the model.

Do not rerun the old supervisor or automatically launch another probe. `runAdaptabilityTraining.mjs` now requires a separately bound successful two-update, longest-example and save/reload proof before spawning main training. Old plans are not valid for the corrected loss implementation. The revised loss retains complete evidence/context, excludes the padding token after the target, and avoids vocabulary projection for masked prompt positions. Synthetic CPU loss, gradient and two-update compiled-Adam equivalence are separate from full-model runtime proof; the latter remains absent. A new measured diagnostic proposal/resource budget and compatible exact runtime proof are the next training prerequisite. No remaining main-run updates have been consumed: 0/48.

The revised 24/8 datasets and every review attempt remain private/versioned; consult STATUS for the latest valid review. Dataset approval is not generated-answer improvement. Restore the 104 service using the existing command when no owned training process is active, and verify real browser content. The service remains for diagnosis with known legal-answer failures, not an invitation to a successful owner acceptance trial.

## Start and stop the actual product

Run from `/Users/hltsang/Desktop/Pensions dashboard demo`. Use the single existing `liveLocal.mjs` supervisor and checkpoint 104. First confirm its ports are free or reuse the already-running owned instance. Do not start a second supervisor, reset demo data, overwrite `.env`, reinstall pinned runtimes, or kill unrelated processes.

```sh
PENSIONS_DB_PATH=/Users/hltsang/.codex/private/pension-live-repair/20260908/development.sqlite \
APPROVED_CORPUS_MANIFEST_PATH=approved-materials/approved-corpus-manifest-20260908-repair-v1.json \
APPROVED_CORPUS_BOOTSTRAP_ON_START=false \
node scripts/liveLocal.mjs \
  --checkpoint training/evaluation-cycle-v2/30-cumulative-legal-training-20260902/checkpoint-selection.json \
  --port 3001
```

Wait for `Pension dashboard is running at http://127.0.0.1:3001`. Open [the assistant](http://127.0.0.1:3001/#assistant). The supervisor owns app 3001, local model 8080 and retrieval 8090; Ctrl-C in that exact supervisor stops its children. Readiness and a running process do not establish legal-answer correctness.

The stated development database already contains the integrity-checked 212-document corpus. Do not use `BOOTSTRAP_ON_START=false` with an old 208-document copy and assume the new manifest has been indexed. A newly prepared database needs the controlled manifest bootstrap and integrity check. The supervisor honors explicit pinned interpreter paths, otherwise selects the local training/retrieval environments.

For owner-authorized private diagnostics, add an absolute `PENSION_DEVELOPMENT_TRACE_ROOT` pointing outside Git. It records query plan, selected source excerpts, declared model output and validation decisions. It is disabled in qualification and cannot be enabled through the chat API. It does not request hidden reasoning. Do not enable raw diagnostics for unrelated users.

## Safe tests

```sh
node scripts/developmentTests.mjs
npm run qualification:test
npm run check
```

The explicit development launcher runs only the allowlisted top-level development suite. The launcher installs a macOS Seatbelt read-denial boundary before test imports, denies the protected suite trees, `test/formal` and the protected draft question file, and tests direct/symlink/child-process denial using a synthetic sentinel. It fails closed without this tested OS boundary. Each invocation prints its exact inventory and private temporary database path.

`qualification:test` runs synthetic controller checks and the separate reviewer/watchdog OS sandbox tests. Those tests do not earn qualification credit. The real bank-audit/preflight tests remain in `test/formal/`; do not invoke them through ordinary globbing. `check` performs JavaScript syntax checks without executing modules or reading bank contents.

Actual development browser checks:

```sh
PLAYWRIGHT_MODULE_PATH=/Users/hltsang/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright/index.js \
PENSION_BROWSER_GROUP=all \
PENSION_BROWSER_OUTPUT=/absolute/private/fresh-browser-directory \
node scripts/liveBrowserChecks.mjs
```

Groups are `healthy`, `legal`, `conversation`, `serving-repair`, `faults`, `dependencies`, `live-cancel` and `all`. The matrix launches real Chrome with isolated contexts and records screenshots, traces, visible replies and request identities. `dependencies` briefly pauses only the verified owned service PIDs and resumes them in `finally`. Keep other model requests stopped during that group. A failed legal prerequisite makes the combined command exit 1 even when resilience checks pass. Never count an expected outage fallback as a correct legal answer.

`liveRepairChecks.mjs` runs owner-visible Q1–Q4 once each through HTTP into a fresh private evidence directory. `developmentModelDiagnosis.mjs OUTPUT A|B [QUESTION]` compares production retrieval with reviewed relevant passages using the same generation and validation functions. Its evidence and system-prompt experiments are CLI-only. They are not production overrides, formal cases or training data. Do not supply protected questions to either command.

## Source and review provenance

New immutable source manifest: `approved-materials/approved-corpus-manifest-20260908-repair-v1.json`, SHA-256 `f559e3ce18ce0ba9cf1128bd3f52d64b44b9faa1d954af93738dec1b3e0f0365`.

The exact source packet, independently isolated A/B outputs, execution transcripts and review receipts are under `approved-materials/live-repair-20260908/review-v3/`. The safety cache expires on 8 October 2026. Its loader verifies package, packet and reviewer-file hashes. An expired/corrupt package serves a labelled basic notice without reviewed citations. Earlier rejected reviews remain preserved. Source admission is not qualification.

## Formal qualification boundary

The canonical configuration now references the new manifest and an isolated future controller database, `data/qualification-repair-20260908.sqlite`. Code/source bindings were extended; protected evaluation inputs, checkpoint, reviewers, permissions, budgets and acceptance standards were retained. Canonical config/runtime digest rotation is documented in the private `configuration-rotation.json`. Old run/configuration evidence was preserved. No historical signature was changed to match new code.

The fresh formal run is **NOT_RUN** until the finite development exit criteria actually pass. Do not claim the known failed Q1 variants passed merely because the validator safely rejected them. The current blocker is described in the model diagnosis. This 8 September restriction is superseded only within the bounded scope of the 9 September owner contract above. Model substitution remains prohibited.

After the prerequisite is genuinely closed, refresh candidate identities and the isolated database as needed, run the existing static preflight, then use the verified controller entrypoints:

```sh
npm run qualification:preflight
npm run qualification:run -- --new-run
```

`--new-run` is accepted only after a prior terminal run. The continuation permits one fresh visible run, not repeated fresh-run loops. Stop on its first formal failure; preserve receipts and do not patch-and-resume under the same candidate.

Keep the established stage order: VERIFY_RUNTIME plus reviewer calibration → T4_TARGETED_REGRESSION → TOPIC161_ORIGINAL_DEVELOPMENT → LIVE50_FULL_REGRESSION → RELIABILITY_GATE → VISIBLE_CRITICAL4 → VISIBLE_FULL69 → VISIBLE_TOPIC161_REPLACEMENT_V2 → VISIBLE_FROZEN13 → CANDIDATE_FREEZE → READY_FOR_SEALED_UNSEEN_OWNER_AUTHORISATION. Sealed unseen needs separate authorization.

## Preservation and rollback

Original data was compared to the initial backup without disclosing row contents; it matched. Private development evidence, uploads, models and uncommitted work remain preserved. Review individual source hunks for rollback; never use a broad reset or replace the original database. Any substantive candidate change invalidates affected downstream receipts. Nothing was deployed or pushed.


## 9 September — Hybrid 實作後的實際續接點

較新狀態以 STATUS 和報告最上方補充為準。當前仍 NOT_READY；checkpoint104 unchanged，v14 serving 的五個法律 development scenario 均未通過。不能以17個正常／韌性 scenario通過來開始formal或owner acceptance。新runtime hashes是development身份，舊CANDIDATE.json不是本次freeze。

當前owned supervisor26863、app26899。不要依賴PID永久有效；每次停止前核對command/parent和port。現有服務可直接沿用，無須重啟。私有根目錄：`/Users/hltsang/.codex/private/pension-live-repair/20260909/hybrid-repair`。`runtime-identity-final.json`綁定model、frontend bytes、DB開檔與服務ready/idle；`process-ownership-final.json`保存所有supervisor角色。先讀該檔，再核對實際process，切勿廣泛pkill。

必要時，使用下列同一開發設定重啟，輸出目錄須新建或刻意接續並標明：

```sh
PENSIONS_DB_PATH=/Users/hltsang/.codex/private/pension-live-repair/20260908/development.sqlite \
PENSION_DEVELOPMENT_TRACE_ROOT=/Users/hltsang/.codex/private/pension-live-repair/20260909/hybrid-repair/traces-final \
DISABLE_DEBUG_LOGGING=false \
LOGGING_ROOT=/Users/hltsang/.codex/private/pension-live-repair/20260909/hybrid-repair/logs-final \
QUALIFICATION_ATTEMPT_TELEMETRY=true \
APPROVED_CORPUS_BOOTSTRAP_ON_START=false \
APPROVED_CORPUS_MANIFEST_PATH=approved-materials/approved-corpus-manifest-20260908-repair-v1.json \
node scripts/liveLocal.mjs --checkpoint training/evaluation-cycle-v2/30-cumulative-legal-training-20260902/checkpoint-selection.json --port 3001
```

`/api/status`、`/api/ready`、`:8080/v1/models`與`:8090/health`確認可用及identity；它們不能替代網頁回答。私有trace包含每次失敗生成的raw user-facing JSON，不對外傳送draft。不要把private traces提交Git或拿protected cases餵入此診斷。

Safe tests仍用上述allowlist launcher；最新253PASS、synthetic controller20PASS。Browser新增`hybrid-targeted`（舊四個失敗情境加owner原句，五個scenario／六requests）。已保存兩次prompt revision和final backend verification，不得重試直到綠燈或只報最佳結果。`healthy`、`faults`、`dependencies`、`live-cancel`各組收據分開；full21 matrix因legal prerequisite未通過，本次沒有另啟一個combined run。Dependencies與live-cancel務必避免其他model请求並行。

失敗畫面/history現在區分`answer_not_verified`、`missing_evidence`、`needs_clarification`、`service_unavailable`，不得把`answer_not_verified`解釋為使用者未輸入portfolio。原始未通過答案仍不可顯示成完成法律指引。顯示用的saved/confirmed/provenance標籤不代表獨立provider核實。

回退：先備份當前diff與development DB；以private `before/` 中這次修改前的對應檔案為參考，逐檔回退本次變更並核對diff。新增檔案及較後修改的preimage需參照各次backup/hash，不能整棵copy或git reset。回退後重新驗證runtime及browser。104 adapter始終未修改，不需要覆寫weights；original DB/uploads不參與回退。

[唯一training resource proposal](live-repair/TRAINING-RESOURCE-PROPOSAL.md)為AWAITING_OWNER_RESOURCE_APPROVAL。可重做no-child `--check-proposal`完整性檢查；不可用它當啟動許可。下一次full-model操作要明確owner資源批准、exact binding、quiet baseline guard、輸入格式equivalence及必要review。正式qualification仍0 fresh attempts；sealed沒有授權。若只續接本次已核查的工作，先讀contract、STATUS、failure-ledger與双review receipts，不重跑reviewed data或另造題庫。


## 最新續作：citation recovery保留，v15撤回，resource proof未啟動

先讀最新report首節、STATUS及private `hybrid-repair/legal-generation-continuation/`，較早的supervisor26863已正常停止。本次active supervisor37031、app37148、exec session17810；以`process-ownership.json`加實際ps/port核對，不永久相信PID。

服務保留unchanged104、原v14 prompt/modelContext及單一citation復原修正。v15的共用checklist/fact-gap指示已因五題法律FAIL及broader混合／失敗结果撤回；完整input/code/review保留於replay-v15、review-v15、broader-v15-final、broader-v15-review。禁止把被撤回版本的較好單題當作final改善。

必要時沿用上方同一啟動命令，但把PENSION_DEVELOPMENT_TRACE_ROOT改為 `/Users/hltsang/.codex/private/pension-live-repair/20260909/hybrid-repair/legal-generation-continuation/live-traces`，LOGGING_ROOT為同root下`live-logs`；private DB路徑及212-source manifest不變。先等supervisor打印完整`Pension dashboard is running`，再確認model ready且idle。僅看到8080 ready可能仍處於owned prefix warm-up；broader harness現已拒絕busy並保存HTTP status/error。

本次開發diagnostic採五個保留trace、相同sources的supplied-context replay；它不代替actual browser。新工具`replayDevelopmentLegalTraces.mjs`只接受明確DEVELOPMENT_ONLY五題manifest與fresh private output，固定104/192tokens/2既有JSON重試，禁用semantic repair。不讀formal/sealed，不自動重試untilgreen。

Resource authority已更新為一次條件額度，**不是generic尚未授權**。原proposal hash不變；數值差異見resource-admissibility.json，NEW_DIAGNOSTIC_ATTEMPTS=0。停own services後swap8615MiB仍超過prestart6144與absolute8192，free67%；不可升cap、停其他app或改全機設定。即使numeric confirmation取得，還需host符合guards及完成proof程式的完整validation、finite/update/frozen-base與reload-generation檢查及必要review/binding；不得憑舊旗標啟動48updates。原兩次失敗仍consumed；main0/48。

Rollback只需核對並恢復本次`before/server/services/localModelService.js`對應hunk，再重啟核驗；不得覆寫整個dirty workspace或originalDB。此前hybrid修復、zero/missing、provenance、source gates與truthful error copy均保留。Formal單次額度尚未消耗，sealed仍沒有授權；owner仍NOT_READY，不能因HTTP200而邀請驗收。
