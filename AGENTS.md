# Project execution instructions

For requested Git updates, target `Tsanhl/Pensiondashboard-model` using `scripts/syncPublicCode.mjs` as documented in `docs/PUBLIC-CODE-SYNC.md`. Do not push the private working branch/history to the public repository. The owner's latest instruction is direct synchronization of the complete public project snapshot to remote `main`, including deletions, without a PR. This Git-specific owner amendment does not authorize deployment, model promotion or unseen access.

For pension-assistant repair/training/qualification, explicitly read [the v3 owner execution contract](docs/OWNER-EXECUTION-CONTRACT.md) and `docs/live-repair/STATUS.json`, then reconcile actual owned processes and receipts before resuming. Preserve existing work and checkpoint 104. Identify protected directories from permitted metadata before search/import/test discovery; sealed unseen remains unauthorised. PDU50-v1 is visible development-only: keep product messages, harness fixtures, evaluator material and model inputs in separate trust roles. Treat 48 as a one-trajectory update cap with proposed 0/12/24/36/48 snapshots, never as an automatic target; do not start training without compatible authority, resource eligibility and the real two-update proof. Use the existing safe development test entrypoint and qualification controller. Training/data review/component tests do not establish browser acceptance or qualification. Preserve all earlier applicable directives.
