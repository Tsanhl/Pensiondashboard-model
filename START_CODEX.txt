Continue my existing Pensions dashboard project using the attached v3 pack.

Read Pension_Dashboard_Full_Execution_Contract_v3.txt in full, including sections26–29 and appendicesE–F. Retain all original evidence/reviewer/resource/unseen protections. Save the versioned owner contract and add only a short AGENTS.md pointer.

First audit why48 was chosen and how the installed trainer counts actual optimiser updates. It is not automatically the best duration. Prepare the one-trajectory capped-at48 comparison with saved candidates at additional updates0/12/24/36/48, using validation for selection and preserving checkpoint104. Do not launch four separate training runs or raise resource caps.

Integrate the supplied PDU50-v1 suite:50 realistic user scenarios, ten follow-ups,38 mixed personal/legal cases. Seed only isolated synthetic development records through the actual mapped two-database repositories. Keep product messages, setup controls and evaluator expectations separate; no gold or expected numeric outputs in model inputs.

Before legal scoring, fetch/bind applicable original sources and review draft acceptance points. Do not claim that a URL, a synthetic statement or this pack's static validator is legal review. Compare actual DB reads, retrieved spans, model input/raw answer, two runtime reviewer receipts and final browser/history/source drawer.

Use PDU50 for matched baseline/selected-candidate development evaluation, not the current training loader or every-checkpoint tuning. Preserve source/fixture/candidate versions. Diagnose failures; fix the first failed boundary. Prepare new reviewed training examples only for demonstrated model-behaviour gaps, and require a separate compatible training authority before another run. Never relabel reused cases as unseen.

Training begins only after valid remaining resource/attempt approval and the real two-update proof. Current legal FAILs are not a pretraining blocker, but must be addressed before promotion. If the new save/eval cadence cannot be covered by existing approved time/call budgets, provide one exact consolidated amendment request while finishing independent work.

Execute permitted work, do not only repeat the plan. Report actual results in Traditional Chinese: update accounting and duration-selection scope, PDU50 source/fixture readiness and layer-level failures, selected adapter or NONE, real browser results, resource blocker, formal gates and UNSEEN=NOT_ACCESSED. No claims of current training success from this authored pack.
